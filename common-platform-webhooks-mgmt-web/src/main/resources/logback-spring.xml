<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <!-- Console Appender with MDC fields -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [traceId=%X{traceId:-N/A} spanId=%X{spanId:-N/A} requestId=%X{requestId:-N/A} eventId=%X{eventId:-N/A} provider=%X{provider:-N/A}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- JSON Appender for production (optional) -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
            <includeMdcKeyName>requestId</includeMdcKeyName>
            <includeMdcKeyName>eventId</includeMdcKeyName>
            <includeMdcKeyName>provider</includeMdcKeyName>
        </encoder>
    </appender>

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

    <!-- Application Loggers -->
    <logger name="com.firefly.common.webhooks" level="DEBUG" />
    <logger name="com.firefly.common.eda" level="DEBUG" />
    <logger name="com.firefly.common.cache" level="INFO" />
    
    <!-- Resilience4j Loggers -->
    <logger name="io.github.resilience4j" level="INFO" />
    
    <!-- Spring Loggers -->
    <logger name="org.springframework" level="INFO" />
    <logger name="org.springframework.web" level="INFO" />
    
    <!-- Kafka Loggers -->
    <logger name="org.apache.kafka" level="WARN" />
</configuration>

